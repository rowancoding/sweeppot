export default function CreatePool() {
  return (
    <main style={{ minHeight: "100vh", background: "var(--dark)", display: "flex", alignItems: "center", justifyContent: "center" }}>
      <div style={{ textAlign: "center", color: "var(--muted)", fontFamily: "var(--font-barlow-condensed), sans-serif" }}>
        <div style={{ fontSize: "2rem", marginBottom: "0.5rem" }}>🚧</div>
        <div style={{ fontSize: "1.1rem", fontWeight: 700, letterSpacing: "0.08em", textTransform: "uppercase" }}>Create Pool — Coming in Session 3</div>
      </div>
    </main>
  );
}
